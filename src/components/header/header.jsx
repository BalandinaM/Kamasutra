import React from 'react';
import h from './header.module.css';

const Header = () => {
  return <header className={h.header}>
    <img src='./img/main-pin.svg' alt='Картинка'/>
  </header>
}

export default Header;
